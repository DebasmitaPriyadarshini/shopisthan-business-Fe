import React from "react";

const ProfileNavBarResponsive = (props) => {
  return (
    <>
      <div className="navbar-for-only-add-product-sbc">{props.children}</div>
    </>
  );
};

export default ProfileNavBarResponsive;
